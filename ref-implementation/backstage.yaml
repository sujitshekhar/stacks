apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backstage
  namespace: argocd
  labels:
    env: dev
spec:
  project: default
  source:
    repoURL: https://github.com/sujitshekhar/stacks
    targetRevision: main
    repoURL: https://github.com/sujitshekhar/stacks
    targetRevision: main
    repoURL: cnoe://backstage/manifests
    targetRevision: HEAD
    path: ref-implementation/ref-implementation/"."
  destination:
    server: "https://kubernetes.default.svc"
    namespace: backstage
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
